%h1 Meeting Management
  
- if flash[:notice] and flash[:notice]!="Signed in successfully."
  #flashNotice
    = flash[:notice]
    
%h4
  = render :partial => "meeting", :locals => { :curr_user => current_user }
  = link_to 'New Meeting', new_meeting_path, class: "btn btn-success"